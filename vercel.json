{
	"version": 2,
	"builds": [
		{
			"src": "api/**/*.js",
			"use": "@vercel/node@2.11.0"
		}
	],
	"routes": [
		{ "src": "/api/(.*)", "dest": "/api/$1" },
		{ "src": "/(.*)", "dest": "/index.html" }
	],
	"outputDirectory": "."
}